# Optional connected-environment experiments. These packages are not required
# for the packaged offline dashboard.
sentence-transformers>=3.0
transformers>=4.40
datasets>=2.19
accelerate>=0.30
torch>=2.1
